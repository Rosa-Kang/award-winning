import React, { useRef } from 'react'

const Navbar = () => {
    const navContainerRef = useRef(null);

  return (
    <div
        ref={navContainerRef}
        className=''
    >
        
    </div>
  )
}

export default Navbar